<%= @post.title%>
<%= @post.content%>
<% @post.categories.each do |category|%>
    <%= category.name%>
    <%end%>

    <%= @post.comments %>
    <% @post.comments.each do |comment| %>
        <%= comment.user.name.uniq %>
        <% end %>

        